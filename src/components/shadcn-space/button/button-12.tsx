import { Button } from "@/components/ui/button";

const ButtonDefaultDemo = () => {
  return (
    <div className="group">
      <Button className="group-hover:-translate-y-1 transition-transform duration-200 cursor-pointer">
        Default
      </Button>
    </div>
  );
};

export default ButtonDefaultDemo;
